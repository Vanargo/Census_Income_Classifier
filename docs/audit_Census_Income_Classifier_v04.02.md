# audit_Census_Income_Classifier_v04.02.md
**Цель:** довести проект до релиза v1.0.0 по итогам аудита v04.  
**Охват:** только невыполненные/частично выполненные задачи из v04 (исключая задачу по `docs/audit_Census_Income_Classifier_v02_new.md`).

---

## A. Репозиторий и документация

### A1. CHANGELOG.md (обязателен для v1.0.0)
**Суть:** ввести журнал изменений по схеме *Keep a Changelog* с семантическим версионированием.  
**Действия:**
1. Создать `CHANGELOG.md` в корне.
2. Принять формат: разделы `Unreleased`, затем релизы в порядке убывания версий; блоки `Added/Changed/Fixed/Removed/Docs/CI`.
3. Заполнить минимум для перехода к `v1.0.0`:
   - Кратко обозначить, что вошло в финализацию: чистые ноутбуки без `outputs`, CI-пайплайн, политика по артефактам, исправления в `paths.py`, консолидация путей, раздел Installation & Reproducibility.
4. Связать `CHANGELOG.md` с релизом `v1.0.0` после создания тега.

**Шаблон (минимум):**
```markdown
# Changelog
All notable changes to this project will be documented in this file.

## [Unreleased]

## [1.0.0] - 2025-11-07
### Added
- Раздел "Installation & Reproducibility" в README.
- CI-пайплайн (линт/формат/тесты).

### Changed
- Единая система путей через `paths.py`.
- Стратегия хранения артефактов (`artifacts/**` игнорируется, исключения документированы).

### Fixed
- Очистка всех ноутбуков от `outputs`.

### Docs
- Обновлён README: инструкции по установке, запуску тестов и ноутбуков.
```

**Критерий готовности (DoD):** файл присутствует, отражает ключевые изменения релиза, валиден по структуре, ссылки на релиз/тег добавлены после публикации.  
**Артефакты:** `CHANGELOG.md`.

---

### A2. README.md — согласованность ссылок и рисунков
**Суть:** выровнять README с фактическими файлами и путями.  
**Действия:**
1. Проверить, что все упомянутые изображения и пути существуют.
2. Убедиться, что раздел **Installation & Reproducibility** актуален и самодостаточен.
3. Добавить ссылку на `CHANGELOG.md` и краткую фразу о семантическом версионировании.
4. Проверить бейдж CI.

**Критерий готовности:** в README нет битых ссылок/картинок, присутствует ссылка на `CHANGELOG.md`, инструкции воспроизводимы.  
**Артефакты:** обновлённый `README.md`.

---

## B. Качество кода и стиль

### B1. Линтинг и форматирование: нулевые замечания `ruff` и `black`
**Суть:** привести код к «чистому» состоянию.  
**Действия:**
1. Прогнать автофиксы:
   ```bash
   ruff check . --fix
   ruff format .
   black .
   ```
2. Если остаются предупреждения без автофиксов, исправить вручную.
3. Прогнать хуки локально:
   ```bash
   pre-commit run -a
   ```

**Критерий готовности:** `ruff check .` и `black .` без замечаний.  
**Артефакты:** исправленные исходники.

---

## C. Версионирование и упаковка

### C1. Версия проекта `1.0.0` в `pyproject.toml`
**Суть:** зафиксировать первую стабильную версию.  
**Действия:**
1. В `pyproject.toml` установить:
   ```toml
   version = "1.0.0"
   ```
2. Проверить заполнение `project.name`, `readme`, `license`, `urls`.

**Критерий готовности:** `pyproject.toml` отражает `1.0.0`.  
**Артефакты:** обновлённый `pyproject.toml`.

---

### C2. Сборка и проверка дистрибутива
**Суть:** убедиться, что проект собирается и устанавливается.  
**Действия:**
1. Собрать:
   ```bash
   python -m build
   ```
2. Установить локально:
   ```bash
   pip install dist/<имя>-1.0.0-py3-none-any.whl
   ```

**Критерий готовности:** сборка и импорт проходят успешно.  
**Артефакты:** `dist/*` (не коммитить).

---

## D. Релиз

### D1. Тег `v1.0.0` и публикация GitHub Release
**Суть:** зафиксировать «золотую» версию.  
**Действия:**
1. Создать аннотированный тег:
   ```bash
   git tag -a v1.0.0 -m "Release v1.0.0"
   git push origin v1.0.0
   ```
2. Создать GitHub Release, использовать текст из `CHANGELOG.md`.

**Критерий готовности:** тег опубликован, CI зелёный.  
**Артефакты:** тег `v1.0.0`, страница релиза.

---

## Итоговая контрольная таблица

| № | Задача | Готовность | Ответственный | Примечание |
|---|---|---|---|---|
| A1 | CHANGELOG.md | ☐ | | Формат Keep a Changelog |
| A2 | README: ссылки/картинки/CI | ☐ | | Добавить ссылку на CHANGELOG |
| B1 | ruff/black: 0 замечаний | ☐ | | Проверить pre-commit |
| C1 | Версия 1.0.0 в pyproject.toml | ☐ | | |
| C2 | Сборка и установка | ☐ | | |
| D1 | Тег и GitHub Release | ☐ | | |