# Release audit — Census Income Classifier v02
Дата анализа: 2025-11-07 11:01:50

## Итоговый вердикт
**НЕ ГОТОВ К РЕЛИЗУ**

Основание:
- Есть блокеры, мешающие корректному открытию кода как 'золотой версии'.

## Обнаруженные блокеры
- [BLOCKER] Отсутствует LICENSE. В заголовках файлов заявлен MIT, но файл лицензии в корне не найден.

## Предупреждения и рекомендации
- [WARN] В архиве присутствуют кеши (__pycache__, .pytest_cache, .ruff_cache). Их надо исключить из репозитория/релиза через .gitignore.
- [WARN] В src/ присутствуют директории *.egg-info. Их не следует коммитить. Добавьте в .gitignore.
- [WARN] Каталог data/artifacts/ и data/processed/ содержит промежуточные артефакты и датасеты. Для GitHub-релиза лучше хранить их как воспроизводимые артефакты (или GitHub Release assets), не в репозитории.
- [WARN] Каталог models/ содержит скомпилированные/сохранённые модели. Проверьте размер и уместность их хранения в репозитории.
- [WARN] .git/ попал в ZIP. Для проверки это не критично, но в релиз-архивы его включать нельзя.
- [WARN] Каталог predictions/ содержит результаты инференса. Обычно их не коммитят, а воспроизводят скриптами.

## Критерии готовности — чек-лист

| Критерий | Найдено | Примечание |
|---|:---:|---|
| README.md | ✅ | Обзор, установка, воспроизводимость |
| requirements.txt | ✅ | Версии зафиксированы |
| environment.yml | ✅ | Совместимо с requirements |
| pyproject.toml | ✅ | Настройки ruff/pytest/setuptools |
| .pre-commit-config.yaml | ✅ | Линтеры и форматтеры |
| CI workflow | ✅ | Линт + тесты по Python 3.10/3.11 |
| LICENSE | ❌ | Файл лицензии в корне |

## Сводка по «лишним» директориям в архиве

- git_dir: 738 элемент(ов)
- pycache: 8 элемент(ов)
- pytest_cache: 7 элемент(ов)
- ruff_cache: 45 элемент(ов)
- egg_info: 5 элемент(ов)
- large_artifacts: 18 элемент(ов)
- processed_data: 2 элемент(ов)
- models_dir: 6 элемент(ов)
- predictions_dir: 2 элемент(ов)
- releases_dir: 84 элемент(ов)

## Наблюдения по ноутбукам
- Все 6 ноутбуков не содержат сохранённых выводов и счетчиков исполнения. Это хорошо для чистоты репозитория.
- Замечание: убедитесь, что README явно ссылается на ноутбуки и порядок их запуска.

## Рекомендованный план доводки до v1.0.0
1) Добавить `LICENSE` (MIT) в корень. Синхронизировать копирайты и бейджи.
2) Расширить `.gitignore`: добавить `.pytest_cache/`, `.ruff_cache/`, `__pycache__/` (если нет), `*.egg-info/`.
3) Перенести объёмные артефакты (`data/artifacts`, `data/processed`, `models`, `predictions`) в GitHub Release assets или генерировать по make/CLI.
4) Проверить `CHANGELOG.md` на опечатки и актуальность даты релиза. Связать с тегом `v1.0.0`.
5) Прогнать CI на чистом клоне; убедиться, что `pytest -q` работает без наличия `micro_model.joblib` (смоук-тесты должны уметь создавать микромодель).
6) Сверить `README` с реальным содержимым notebooks/src/tests и указать лимиты по памяти/времени.

> Полный перечень файлов доступен в CSV-манифесте (ссылка ниже).